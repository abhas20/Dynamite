{
    "name": "dynamite-cli-blast",
    "version": "0.0.1",
    "private": true,
    "type": "module",

    "scripts": {
        "dev": "pnpm -w -r --parallel --filter \"./client...\" --filter \"./server...\" run dev",
        "build": "pnpm -w -r --parallel --filter \"./client...\" --filter \"./server...\" run build",
        "start": "pnpm -w -r --filter \"./server...\" run start",
        "lint": "pnpm -w -r --parallel --filter \"./client...\" --filter \"./server...\" run lint"
    },

    "dependencies": {},

    "devDependencies": {}
}